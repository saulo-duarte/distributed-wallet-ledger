global:
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:
  - job_name: "goledge-api"
    metrics_path: "/metrics"
    static_configs:
      - targets: ["host.docker.internal:8082", "goledge-api:8082", "localhost:8082"]
