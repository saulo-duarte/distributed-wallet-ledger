apiVersion: v1
kind: Service
metadata:
  name: {{ include "goledge.fullname" . }}
  labels:
    {{- include "goledge.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "goledge.selectorLabels" . | nindent 4 }}
