// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: wallets.sql

package db

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const createWallet = `-- name: CreateWallet :one
INSERT INTO wallets (
    id,
    owner_id,
    ledger_account_id,
    currency,
    status
)
VALUES (
    $1,
    $2,
    $3,
    $4,
    $5
)
RETURNING
    id,
    owner_id,
    ledger_account_id,
    currency,
    status,
    created_at
`

type CreateWalletParams struct {
	ID              pgtype.UUID `json:"id"`
	OwnerID         string      `json:"owner_id"`
	LedgerAccountID pgtype.UUID `json:"ledger_account_id"`
	Currency        string      `json:"currency"`
	Status          string      `json:"status"`
}

func (q *Queries) CreateWallet(ctx context.Context, arg CreateWalletParams) (Wallet, error) {
	row := q.db.QueryRow(ctx, createWallet,
		arg.ID,
		arg.OwnerID,
		arg.LedgerAccountID,
		arg.Currency,
		arg.Status,
	)
	var i Wallet
	err := row.Scan(
		&i.ID,
		&i.OwnerID,
		&i.LedgerAccountID,
		&i.Currency,
		&i.Status,
		&i.CreatedAt,
	)
	return i, err
}

const getWalletByID = `-- name: GetWalletByID :one
SELECT
    id,
    owner_id,
    ledger_account_id,
    currency,
    status,
    created_at
FROM wallets
WHERE id = $1
`

func (q *Queries) GetWalletByID(ctx context.Context, id pgtype.UUID) (Wallet, error) {
	row := q.db.QueryRow(ctx, getWalletByID, id)
	var i Wallet
	err := row.Scan(
		&i.ID,
		&i.OwnerID,
		&i.LedgerAccountID,
		&i.Currency,
		&i.Status,
		&i.CreatedAt,
	)
	return i, err
}

const listWalletsByOwnerID = `-- name: ListWalletsByOwnerID :many
SELECT
    id,
    owner_id,
    ledger_account_id,
    currency,
    status,
    created_at
FROM wallets
WHERE owner_id = $1
ORDER BY created_at DESC, id DESC
`

func (q *Queries) ListWalletsByOwnerID(ctx context.Context, ownerID string) ([]Wallet, error) {
	rows, err := q.db.Query(ctx, listWalletsByOwnerID, ownerID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []Wallet{}
	for rows.Next() {
		var i Wallet
		if err := rows.Scan(
			&i.ID,
			&i.OwnerID,
			&i.LedgerAccountID,
			&i.Currency,
			&i.Status,
			&i.CreatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const lockWalletByID = `-- name: LockWalletByID :one
SELECT
    id,
    owner_id,
    ledger_account_id,
    currency,
    status,
    created_at
FROM wallets
WHERE id = $1
FOR UPDATE
`

func (q *Queries) LockWalletByID(ctx context.Context, id pgtype.UUID) (Wallet, error) {
	row := q.db.QueryRow(ctx, lockWalletByID, id)
	var i Wallet
	err := row.Scan(
		&i.ID,
		&i.OwnerID,
		&i.LedgerAccountID,
		&i.Currency,
		&i.Status,
		&i.CreatedAt,
	)
	return i, err
}

const lockWalletByLedgerAccountID = `-- name: LockWalletByLedgerAccountID :one
SELECT
    id,
    owner_id,
    ledger_account_id,
    currency,
    status,
    created_at
FROM wallets
WHERE ledger_account_id = $1
FOR UPDATE
`

func (q *Queries) LockWalletByLedgerAccountID(ctx context.Context, ledgerAccountID pgtype.UUID) (Wallet, error) {
	row := q.db.QueryRow(ctx, lockWalletByLedgerAccountID, ledgerAccountID)
	var i Wallet
	err := row.Scan(
		&i.ID,
		&i.OwnerID,
		&i.LedgerAccountID,
		&i.Currency,
		&i.Status,
		&i.CreatedAt,
	)
	return i, err
}
